# deebee
A system for storing values similarly to the settings class in PHP-CLI, but in memory and runs inside communicator_server, and flushed to a sqlite database every 10 seconds when loaded by communicator_server.
